fastapi>=0.115.0
playwright>=1.40.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
python-dotenv>=1.0.0
pyttsx3>=2.90
psutil>=5.9.0
Pillow>=10.0.0
discord.py>=2.0.0

# Voice cloning (optional — ~1.8 GB model download on first use)
# TTS>=0.22.0          # Coqui XTTS v2 — pip install TTS
# Requires: pip install torch  (CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu)

# Audio conversion (needed for WebM→WAV from browser recordings)
# Install ffmpeg separately: winget install ffmpeg

# Optional cloud TTS / LLM
# openai>=1.0.0
# anthropic>=0.39.0
